# Course changelog

Append-only ledger of decisions and batch progress. **Add new rows; never edit
or reorder past ones.** Durable conventions distilled from this log live in
`conventions.md`. Every session that changes content or process appends a row
(see `CLAUDE.md`, contract #6). When this repo is cloned to start a course,
keep the rules above and start your own table below them.

| Date | Decision / Update |
| --- | --- |
| 2026-08-02 | **Kit repository created** (extracted from PHYS 567; design record in the donor repo's `streamlining.md` §8–§9). Skeleton: layout, README, agent manual (`CLAUDE.md` with the binding contract: prose fidelity, math "show don't tell", single source, alt-at-birth, flagging + ledger discipline), guide stubs, this ledger. Acceptance test: donor lecture 25 re-authored in kit format, both targets clean through the full gates. |
| 2026-08-02 | **A1+A2 landed and smoke-tested: the shared layer, both drivers, and a worked lecture00 — one content file builds both targets, no transform.** `shared/`: `course.tex` (single-file course identity), `macros.tex` starter (with growth conventions), `preamble-slides.tex` (extracted from the donor beamer preamble: palette, theme, `\fig`/`\case`/announcements), `preamble-web.tex` (the donor's shim layer collapsed into classic TeX — no xparse/expl3; BookML loaded guarded with `[nomathjax,style=plain]` + `\bmlImageEnvironment{tikzpicture}`; `\case`→"(N)"; alt registry + `\usealtfrom`; announcements no-op'd unless `\kitkeepannouncements`). `drivers/`: thin wrappers selecting the lecture via `\kitlecture`; web driver auto-loads the lecture's alt sidecar. Worked example: `content/lecture00.tex` exercising frames/blocks/columns/overlays/`\case`/align/`\fig` + `figures/lecture00/square-lattice` + `alt/lecture00.tex` + `announcements/lecture00.tex`. **Build wiring:** root `GNUmakefile` sets `SOURCES := $(wildcard lecture*.tex)` explicitly — BookML's auto-scan greps for a literal `\documentclass`, which the generated 2-line wrappers don't contain; `bookml/` is git-ignored (fetched at a pinned release, not vendored). **Smoke results:** slides = 4-page PDF, donor theme intact, announcements sidecar renders, overlay reveals work; web = BookML end-to-end, **pa11y clean ×4 pages, bare-math-text 0**, figure as `<img class='ltx_graphics'>`, sections + TOC native. **Two lessons re-learned from the donor pilot:** LaTeXML re-establishes `\titlepage` after the preamble — override must be `\AtBeginDocument`; and `\newcommand\foo#1` is invalid (use `[1]`). **Known gaps, by design:** `alt=""` until the A3 binding lands; one `dpth` depth error until setup installs ghostscript-with-libgs (A5); LaTeXML footer credit present (cosmetic; revisit at A4). |
| 2026-08-02 | **A3+A4 landed: alt-text wiring and the one-command driver — the kit's gate suite now runs on every build.** **A3:** `shared/kitalt.sty` (pdflatex no-op) + `shared/kitalt.sty.ltxml` (a ~20-line LaTeXML binding: `\kitfigdesc{text}` sets `description` on the most recent `ltx:graphics`, which serializes as `<img alt>`); `\fig` reordered so the hook runs after the image node exists. Verified: the lecture00 figure carries its full sidecar description in `alt`; pa11y stays clean. **A4:** `course.py` (stdlib-only) — verbs `build` (wrapper generation with change-detection so make caching survives; BookML target under `AUX_DIR=build/web`; check suite auto-runs), `slides` (latexmk into `build/slides`, beamer extras tidied; announcements always included by design), `check` (latexml-log errors with the libgs/depth class told apart and downgraded-with-remediation when dvisvgm lacks Ghostscript; `[ALT MISSING]`; EMPTY `alt` on figure `<img>`s; the donor bare-math-text counter; unparsed-math count; pa11y per page with macOS browser auto-detection), `index` (site index scraped from `\kitlecturetitle` in content/ — **no lectures.tsv manifest in the kit**, the single source covers it), `doctor`, `clean [--deep]` (bmlimages figure cache preserved by default). **Toolchain finding: the kit needs NO pinning** — the full pipeline (pdflatex, preview/comment, dvisvgm 3.6, LaTeXML 0.8.8) runs green on stock TeX Live 2026; the donor's TL2025 pin was a tex4ht constraint the kit doesn't inherit. `doctor` verifies presence (and explains the libgs gap); `KIT_TEXBIN` exists for machines that do need a pin. Verified: `doctor` / `build 00` PASS (math 5, unparsed 0, figures 1, bare-math-text 0, pa11y clean ×4) / `slides 00` / `index` (pa11y clean) all green. Remaining in group A: setup script (A5), site styling pass (cards/badges — lands against real content), guide chapters (A6). |
| 2026-08-02 | **A5+A6 landed: the setup script and the written-down experience — group A complete except the styling pass.** **A5:** `setup.sh` (idempotent; asks before every install; `--yes`/`--dry-run`): TeX Live presence, `preview`/`comment` via tlmgr, LaTeXML + Ghostscript via brew where available, **BookML fetched at the pinned release with sha256 verification** (v0.29.4, `1ec8c79f…`; version detected via `$bmlVersion` in the binding for idempotence), optional pa11y; finishes with `doctor`. `course.py` now **auto-wires `LIBGS`** to a Homebrew/system `libgs.dylib` on macOS so dvisvgm gains Ghostscript support the moment it's installed (user-set `LIBGS` wins; positive path untested on this machine — no ghostscript library present — flagged for first verification after an install). Dry-run verified: all present-checks green, the ghostscript gap correctly surfaced as the only would-ask, BookML idempotence detected. **A6:** the four guide chapters written from donor distillation — `setup.md` (template instantiation, toolchain table, libgs/BookML-pin/browser/Windows wrinkles), `authoring.md` (file anatomy, the construct palette with per-target renderings, math rules incl. equation-breaking patterns and the balanced-fence corruption trap, alt-text ownership + quality guidance, the banned list with reasons, macro promotion), `workflow.md` (S1→S4 batches with gates, the day-of-lecture loop, publishing, the regression stance, ledger duties), `troubleshooting.md` (**the tripwire table**: 14 symptom→meaning→fix rows, each encoding a donor failure). **`CLAUDE.md` is now fully operational** — the ⟨extraction pending⟩ markers replaced with complete S1→S4 stage instructions (batch sizes, per-stage review boundaries, the donor's staging discipline) and the gate section wired to `course.py` + the fidelity review + the ledger-row-or-not-done rule. README status current. **Group A remaining: the site styling pass only** (slide cards/badges — deliberately deferred to land against real content in group B). |
| 2026-08-02 | **Lecture 25 authored end-to-end in kit format (S1→S4) — the acceptance test set at repo creation, and it passes on both targets.** Donor lecture 25 re-authored from its 13-page handwritten source into `content/lecture25.tex`: 8 sections / 20 content frames covering the honeycomb lattice, the enforced symmetries and the space group `p6/mmm1'`, the reciprocal lattice + TRIMs, the band representation (`B(I)`, `B(C_6)`, `B(\TRS)`), the nearest-neighbour model, and the graphene Dirac point; plus 5 TikZ figures with `alt/lecture25.tex`, and `announcements/lecture25.tex` (verbatim, slides-only). **Gates:** `build 25` PASS — math 206, unparsed 2 (~1%), figures 5, bare-math-text 0, pa11y clean ×8 pages, zero `[ALT MISSING]`; `slides 25` PASS — 28 pages incl. overlays; zero overfull boxes on either target. **Decisions, all now in `conventions.md`:** (1) **bordered/labelled matrices REJECTED on accessibility grounds** — `\bordermatrix`'s floating row/column labels are visual-spatial information that reads badly or not at all for a screen-reader user, *and* they drop the formula out of parsed MathML (measured: unparsed 2→6 when used, back to 2 when removed); house rule is a plain `pmatrix` with the basis named in the introducing sentence ("in the sublattice basis $(A,B)$, …"), which serves every reader identically. (2) Boxed board callouts are emphasis → `\alert`, never a titled `block` — inventing a title to justify a block breaches prose fidelity. (3) Write `\otimes` explicitly even where the source juxtaposes factors. (4) New **Notation (physics)** section: Pauli *superscripts*, `\TRS`/`\conj`, `\sg{...}` (lecture-local in 25; promote to `shared/macros.tex` as `\providecommand` when lecture 26 wants it), a mirror's subscript names its plane *normal* (keep the Hermann–Mauguin index — students look the group up on the Bilbao server), "irreps". (5) New **Frame structure** section: `\announcementsframe` after the title frame; every lecture ends with a *static* Takeaways frame. (6) **Alt text is richly descriptive and carries what the figure *argues***, not just what it depicts; conversely, board thinking-marks (traversal arrows, lassos) are *not* reproduced — reproduce what the drawing says, not every pen stroke. (7) Two figures showing the same path must label it identically; symmetry-equivalent representatives are a free choice, which is exactly why the choice has to agree across figures. **Deviations from the source, all instructor-directed and deliberate:** the p10 inversion constraint `⟨Bσ −R|H|AσR⟩ = t` is **wrong** (a board slip) and is transcribed *corrected* as `⟨AσR|H|BσR⟩ = t`, with one connecting line of derivation; the p2 bottom 4-band Wilson pair dropped; the p12 Brillouin-zone-path inset folded into the band-structure figure (6 figures → 5), whose horizontal axis now *is* that path; frame 8 re-laid with `columns` — matching the source page's own left/right layout — rather than split. Progressive reveals added to the three heavy derivation frames only; intro/recap/Takeaways left static. **Two kit findings, both added to `docs/troubleshooting.md`:** a bare palette colour in a TikZ node's option list silently overrides a `fill=white` coming from the node style (because `color=` sets the fill too), so white-backed figure labels render as solid coloured boxes — colour text with `text=<colour>`; and TikZ `scale=` does **not** scale text nodes, so shrinking a figure to fit a frame is not a free density fix (re-lay the frame instead). Resumable stage record: `notes-lecture25.md`. |
| 2026-08-02 | **Acceptance test COUNTERSIGNED — the kit ships.** Instructor comparison of the isolated-agent lecture 25 against the donor course's validated version: content parity total on every probe (all formulas, conventions, and the corrected inversion constraint — replayed through pass-2 review exactly as the donor's original fix); structure equivalent at finer grain; figures 5/5 content-correct, in places MORE source-faithful than the donor. All divergences ruled within tolerance ("different choices") — and one ran the other way: **the kit's computed band-structure figure exposed a periodicity bug in the donor's hand-drawn one** (K→Γ′ ending at ±2t, not ±3t), now backported to the donor course. Lessons recorded: conventions.md must carry cross-lecture TERMINOLOGY and presentation preferences (house tables, house names like "sewing matrix"), not just notation, or a faithful fresh agent reproduces the source instead of the house style. **Flagged improvements applied + tested:** `\figw{width}{path}{key}` (adjustbox cap on slides — scales text, unlike TikZ `scale=`; web ignores the width), `course.py figure NN name` (standalone single-figure render, PDF+PNG — the fast S3 loop), `shared/palette.tex` as the single colour source, `notes/lectureNN.md` promoted to contract #7 + the documented layout, lecture00-deletion warning in the publishing guide. **Template stance (instructor):** the lecture-25 fixture does NOT ship — the template stays course-agnostic; the fixture lives untracked (`.git/info/exclude`, deliberately not `.gitignore`, so cloned courses can track their own lecture 25) for internal kit testing. `shared/course.tex` restored to placeholders. |
| 2026-08-04 | **Site styling pass (instructor decision: a light CSS layer over BookML plain).** New `shared/kit.css`, loaded last via `LATEXMLPOSTEXTRAFLAGS += --css=shared/kit.css` in the GNUmakefile (latexmlpost copies it into each lecture's output, preserving the `shared/` path). The layer: a 46rem reading column with serif body type; **every frame a card** (`section.ltx_subsection` — border, palette-blue left accent, rounded corners); sans palette-blue headings with the section number in orange; the prev/next header and footer restyled as rounded gray chrome, the header `position:sticky`; figure max-width, booktabs-flavoured `ltx_tabular` padding, muted footer credit. Palette mirrors `shared/palette.tex` (keep in sync). **Lesson caught live in a dark-mode browser: LaTeXML's stock CSS carries a `prefers-color-scheme: dark` variant** that sat the light palette on a black page (the section headings were near-illegible) — fixed by pinning a **single forced-light scheme** (`color-scheme: light` + unqualified `html/body` background rules that win by cascade order); a real dark theme needs an SVG figure-palette strategy first and stays deferred. "Slide n" badges deliberately NOT attempted in CSS: BookML splits lectures across per-section pages, so CSS counters would reset per page and number wrongly — badges need a post-pass if ever wanted. Verified: lecture00 + lecture25 fixture rebuilt PASS, **pa11y clean on every page with the new palette**, sticky header + forced-light confirmed in a live dark-mode browser, site index regenerated clean. README status updated. |
