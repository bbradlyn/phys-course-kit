# Course changelog

Append-only ledger of decisions and batch progress. **Add new rows; never edit
or reorder past ones.** Durable conventions distilled from this log live in
`conventions.md`. Every session that changes content or process appends a row
(see `CLAUDE.md`, contract #6). When this repo is cloned to start a course,
keep the rules above and start your own table below them.

| Date | Decision / Update |
| --- | --- |
| 2026-08-02 | **Kit repository created** (extracted from PHYS 567; design record in the donor repo's `streamlining.md` §8–§9). Skeleton: layout, README, agent manual (`CLAUDE.md` with the binding contract: prose fidelity, math "show don't tell", single source, alt-at-birth, flagging + ledger discipline), guide stubs, this ledger. Acceptance test: donor lecture 25 re-authored in kit format, both targets clean through the full gates. |
| 2026-08-02 | **A1+A2 landed and smoke-tested: the shared layer, both drivers, and a worked lecture00 — one content file builds both targets, no transform.** `shared/`: `course.tex` (single-file course identity), `macros.tex` starter (with growth conventions), `preamble-slides.tex` (extracted from the donor beamer preamble: palette, theme, `\fig`/`\case`/announcements), `preamble-web.tex` (the donor's shim layer collapsed into classic TeX — no xparse/expl3; BookML loaded guarded with `[nomathjax,style=plain]` + `\bmlImageEnvironment{tikzpicture}`; `\case`→"(N)"; alt registry + `\usealtfrom`; announcements no-op'd unless `\kitkeepannouncements`). `drivers/`: thin wrappers selecting the lecture via `\kitlecture`; web driver auto-loads the lecture's alt sidecar. Worked example: `content/lecture00.tex` exercising frames/blocks/columns/overlays/`\case`/align/`\fig` + `figures/lecture00/square-lattice` + `alt/lecture00.tex` + `announcements/lecture00.tex`. **Build wiring:** root `GNUmakefile` sets `SOURCES := $(wildcard lecture*.tex)` explicitly — BookML's auto-scan greps for a literal `\documentclass`, which the generated 2-line wrappers don't contain; `bookml/` is git-ignored (fetched at a pinned release, not vendored). **Smoke results:** slides = 4-page PDF, donor theme intact, announcements sidecar renders, overlay reveals work; web = BookML end-to-end, **pa11y clean ×4 pages, bare-math-text 0**, figure as `<img class='ltx_graphics'>`, sections + TOC native. **Two lessons re-learned from the donor pilot:** LaTeXML re-establishes `\titlepage` after the preamble — override must be `\AtBeginDocument`; and `\newcommand\foo#1` is invalid (use `[1]`). **Known gaps, by design:** `alt=""` until the A3 binding lands; one `dpth` depth error until setup installs ghostscript-with-libgs (A5); LaTeXML footer credit present (cosmetic; revisit at A4). |
| 2026-08-02 | **A3+A4 landed: alt-text wiring and the one-command driver — the kit's gate suite now runs on every build.** **A3:** `shared/kitalt.sty` (pdflatex no-op) + `shared/kitalt.sty.ltxml` (a ~20-line LaTeXML binding: `\kitfigdesc{text}` sets `description` on the most recent `ltx:graphics`, which serializes as `<img alt>`); `\fig` reordered so the hook runs after the image node exists. Verified: the lecture00 figure carries its full sidecar description in `alt`; pa11y stays clean. **A4:** `course.py` (stdlib-only) — verbs `build` (wrapper generation with change-detection so make caching survives; BookML target under `AUX_DIR=build/web`; check suite auto-runs), `slides` (latexmk into `build/slides`, beamer extras tidied; announcements always included by design), `check` (latexml-log errors with the libgs/depth class told apart and downgraded-with-remediation when dvisvgm lacks Ghostscript; `[ALT MISSING]`; EMPTY `alt` on figure `<img>`s; the donor bare-math-text counter; unparsed-math count; pa11y per page with macOS browser auto-detection), `index` (site index scraped from `\kitlecturetitle` in content/ — **no lectures.tsv manifest in the kit**, the single source covers it), `doctor`, `clean [--deep]` (bmlimages figure cache preserved by default). **Toolchain finding: the kit needs NO pinning** — the full pipeline (pdflatex, preview/comment, dvisvgm 3.6, LaTeXML 0.8.8) runs green on stock TeX Live 2026; the donor's TL2025 pin was a tex4ht constraint the kit doesn't inherit. `doctor` verifies presence (and explains the libgs gap); `KIT_TEXBIN` exists for machines that do need a pin. Verified: `doctor` / `build 00` PASS (math 5, unparsed 0, figures 1, bare-math-text 0, pa11y clean ×4) / `slides 00` / `index` (pa11y clean) all green. Remaining in group A: setup script (A5), site styling pass (cards/badges — lands against real content), guide chapters (A6). |
